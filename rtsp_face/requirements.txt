# Video face scanner — uniface-scanner branch
# Python 3.12, CUDA 12.x, cuDNN 9.x

# Core
uniface[gpu]==3.1.1
numpy==2.4.3
opencv-python==4.13.0.92

# ONNX Runtime — GPU version required for CUDA acceleration.
# Pin to 1.20.x: versions >= 1.21 have a broken CUDA EP plugin loading
# on Linux (CUDAExecutionProvider absent from get_available_providers()),
# causing silent CPU fallback. Do not upgrade without testing first.
onnxruntime-gpu==1.20.1
rapidocr_onnxruntime

# Web browser
flask>=3.0

# Testing
pytest==9.0.2
